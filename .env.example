# WiNet Extractor Environment Example
#
# Basic multi-inverter configuration (recommended)
# WINET_HOSTS: comma-separated list of Sungrow inverter IP addresses
# WINET_NAMES: matching list of friendly names (optional; defaults to inverter1, inverter2,...)
# Example:
#   WINET_HOSTS=192.168.1.100,192.168.1.101
#   WINET_NAMES=garage_roof,workshop_roof
WINET_HOSTS=192.168.1.114,192.168.1.115
WINET_NAMES=garage,roof

# Optional single-inverter fallback (used if WINET_HOSTS is empty)
# WINET_HOST=192.168.1.12

# Modbus TCP configuration (per inverter). Provide an IP when a Modbus smart meter is connected.
# Use an empty entry for inverters without a meter.
# Example: MODBUS_IPS=192.168.1.12,
MODBUS_IPS=,

# Inverter family selection (used for register defaults) — choose STRING or HYBRID
# If omitted, defaults to STRING.
INVERTER_TYPE=STRING

# MQTT broker configuration
MQTT_URL=mqtt://192.168.1.101:1883
# MQTT_USER=your_mqtt_username
# MQTT_PASS=your_mqtt_password

# WiNet authentication (leave blank to use inverter defaults)
WINET_USER=admin
WINET_PASS=pw8888

# Polling interval in seconds
POLL_INTERVAL=10

# Use HTTPS/WSS when available (self-signed accepted by default)
SSL=true

# Optional: control certificate validation
# SSL_VALIDATION=bypass   # accept any certificate (default behaviour)
# SSL_VALIDATION=pinned   # require matching fingerprints below
# SSL_VALIDATION=strict   # full CA validation (most Sungrow inverters will fail)
# INVERTER_CERT_FINGERPRINTS=sha256:...

# Home Assistant discovery prefix
HA_PREFIX=homeassistant/sensor

# Disable PostHog analytics (set to true to opt out)
ANALYTICS=false

# Optimise sensor set for single-phase systems (skips unused phases)
SINGLE_PHASE_SYSTEM=false

# Host timezone
TZ=Australia/Sydney
